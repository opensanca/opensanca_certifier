<h1>Create the Event</h1>

<%= simple_form_for [:admin, @event] do |form| %>
  <%= form.input :title %>
  <%= form.input :meetup_url %>
  <%= form.button :submit %>
  <%= link_to "List Events", "/admin/events", :class => "btn btn-default"  %>
<% end %>
